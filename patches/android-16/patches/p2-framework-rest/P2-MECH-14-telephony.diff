diff --git a/src/java/com/android/internal/telephony/PhoneSubInfoController.java b/src/java/com/android/internal/telephony/PhoneSubInfoController.java
index 1805ac3f41..9ae247219e 100644
--- a/src/java/com/android/internal/telephony/PhoneSubInfoController.java
+++ b/src/java/com/android/internal/telephony/PhoneSubInfoController.java
@@ -40,6 +40,8 @@ import android.os.SystemProperties;
 import android.os.TelephonyServiceManager.ServiceRegisterer;
 import android.telephony.ImsiEncryptionInfo;
 import android.telephony.PhoneNumberUtils;
+import android.os.SystemProperties;
+import android.util.BstUtils;
 import android.telephony.SubscriptionManager;
 import android.telephony.TelephonyFrameworkInitializer;
 import android.telephony.TelephonyManager;
@@ -63,6 +65,9 @@ import java.util.List;
 import java.util.stream.Collectors;
 
 public class PhoneSubInfoController extends IPhoneSubInfo.Stub {
+    // A16DBG:P2:MECH BST telephony device-id gate (a13)
+    private static final boolean BST_TELEPHONY_CHANGES_ENABLED =
+        SystemProperties.getBoolean("bst.config.enable_telephony", true);
     private static final String TAG = "PhoneSubInfoController";
     private static final boolean DBG = true;
     private static final boolean VDBG = false; // STOPSHIP if true
@@ -105,6 +110,10 @@ public class PhoneSubInfoController extends IPhoneSubInfo.Stub {
 
     public String getDeviceIdForPhone(int phoneId, String callingPackage,
             String callingFeatureId) {
+        // A16DBG:P2:MECH BST: fake device-id from property (a13; anti-detection)
+        if (BST_TELEPHONY_CHANGES_ENABLED) {
+            return SystemProperties.get("bst.imei_id", "");
+        }
         enforceCallingPackageUidMatched(callingPackage);
         return callPhoneMethodForPhoneIdWithReadDeviceIdentifiersCheck(phoneId, callingPackage,
                 callingFeatureId, "getDeviceId", (phone) -> phone.getDeviceId());
