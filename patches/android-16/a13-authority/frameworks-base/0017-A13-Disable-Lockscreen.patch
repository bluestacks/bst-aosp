From 01487c02a894fa2b09ca4f2f0636e317a0368749 Mon Sep 17 00:00:00 2001
From: muzhong-bst <muzhong@bluestacks.com>
Date: Thu, 14 Sep 2023 14:40:06 +0800
Subject: [PATCH 017/224] [A13] Disable Lockscreen

---
 core/java/com/android/internal/widget/LockPatternUtils.java | 6 ++++++
 1 file changed, 6 insertions(+)

diff --git a/core/java/com/android/internal/widget/LockPatternUtils.java b/core/java/com/android/internal/widget/LockPatternUtils.java
index 2dfe89397ea5..ea746772025f 100644
--- a/core/java/com/android/internal/widget/LockPatternUtils.java
+++ b/core/java/com/android/internal/widget/LockPatternUtils.java
@@ -633,6 +633,7 @@ public class LockPatternUtils {
      */
     @UnsupportedAppUsage
     public boolean isLockScreenDisabled(int userId) {
+        /*
         if (isSecure(userId)) {
             return false;
         }
@@ -645,6 +646,8 @@ public class LockPatternUtils {
         return getBoolean(DISABLE_LOCKSCREEN_KEY, false, userId)
                 || (disabledByDefault && !isSystemUser)
                 || isDemoUser;
+        */
+        return true;
     }
 
     /** Returns if the given quality maps to an alphabetic password */
@@ -953,8 +956,11 @@ public class LockPatternUtils {
 
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
     public boolean isLockPasswordEnabled(int userId) {
+        /*
         int type = getCredentialTypeForUser(userId);
         return type == CREDENTIAL_TYPE_PASSWORD || type == CREDENTIAL_TYPE_PIN;
+        */
+        return false;
     }
 
     /**
