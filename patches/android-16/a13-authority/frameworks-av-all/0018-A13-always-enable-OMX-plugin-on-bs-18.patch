From 0bddd694da15cc377aad4ef4be40652caa258882 Mon Sep 17 00:00:00 2001
From: allen-bst <70563899+allen-bst@users.noreply.github.com>
Date: Tue, 19 Dec 2023 16:24:27 +0800
Subject: [PATCH 18/21] [A13] always enable OMX plugin on bs (#18)

---
 media/libstagefright/omx/OMXStore.cpp | 12 ++----------
 1 file changed, 2 insertions(+), 10 deletions(-)

diff --git a/media/libstagefright/omx/OMXStore.cpp b/media/libstagefright/omx/OMXStore.cpp
index 243a7df30a..7d62f66519 100644
--- a/media/libstagefright/omx/OMXStore.cpp
+++ b/media/libstagefright/omx/OMXStore.cpp
@@ -131,21 +131,13 @@ static bool isTV() {
     return kIsTv;
 }
 
-static bool isAppPlayer() {
-    std::string instance = android::base::GetProperty("bst.instance", "");
-
-    if (!instance.empty() && instance == "Tiramisu64") {
-        return true;
-    }
-
-    return false;
-}
 
 void OMXStore::addPlugin(OMXPluginBase *plugin) {
     Mutex::Autolock autoLock(mLock);
 
     bool typeTV = isTV();
-    bool appPlayer = isAppPlayer();
+    //In the environment of BlueStacks App Player, can not overlook the OMX plugin.
+    bool appPlayer = true;
     int firstApiLevel = getFirstApiLevel();
 
     OMX_U32 index = 0;
